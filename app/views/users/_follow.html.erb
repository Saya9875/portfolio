<%= form_with(model: current_user.active_relationships.build, remote: true) do |f| %>
  <div><%= hidden_field_tag :followed_id, @user.id %></div>
  <%= f.submit "フォローする", class: "w-40 flex items-center justify-center border-purple-700 border-2 rounded-full focus:outline-none focus:border-purple-800 px-4 py-2  text-purple-800 hover:bg-purple-800 hover:text-white" %>
<% end %>