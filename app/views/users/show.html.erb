<p>MyPage  <%= @user.name %><%= "さん こんにちは" %></p>
<%= @user.email %>

<div class="grid grid-cols-1">
    <% if @user.microposts.any? %>
      <h3>Microposts (<%= @user.microposts.count %>)</h3>
      <ol class="microposts">
        <%= render @microposts %>
      </ol>
    <% end %>
</div>